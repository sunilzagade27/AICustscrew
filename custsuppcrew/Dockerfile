# Python runtime image for CrewAI API and stub telemetry.
# Host publish stays on 127.0.0.1 (see docker-compose.yml). In-container listen is 0.0.0.0.

FROM python:3.12-slim

COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv

WORKDIR /app

ENV UV_COMPILE_BYTECODE=1 \
    UV_LINK_MODE=copy \
    PATH="/app/.venv/bin:$PATH"

COPY pyproject.toml uv.lock README.md ./
COPY src ./src

RUN uv sync --frozen --no-dev --no-editable

EXPOSE 8000 8081

# Default is the investigation API. Compose overrides command for stubs.
CMD ["uvicorn", "custsuppcrew.api:app", "--host", "0.0.0.0", "--port", "8000"]
