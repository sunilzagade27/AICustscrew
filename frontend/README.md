# AICustscrew Frontend — Critical Research Workflow

React + TypeScript (Vite) single-route MVP UI.

## Run

```bash
cd frontend
npm install
npm run dev
```

Opens on http://127.0.0.1:3000/

## Spec

See `../project-context/2.build/frontend-functional-spec.md` (Inputs, Run, Results, History + Spec Sync checklist).
